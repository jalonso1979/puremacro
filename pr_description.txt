🎯 **What:** The `realized_jump` function lacked tests with simple static mock values. The function calculates relative jump RJ = max(0, RV - BV) / RV and includes basic bounds checking.

📊 **Coverage:** Added simple mock value testing in `test_realized_vol_coverage.py` using simple bounds and limits. Includes scenarios:
* Normal valid computation `rv=10.0, bv=5.0`
* Zero jump because equal bounds `rv=10.0, bv=10.0`
* Negative jump bounded to zero `rv=10.0, bv=15.0`
* Division by zero protection `rv=0.0, bv=5.0`
* Negative RV fallback `rv=-5.0, bv=5.0`

✨ **Result:** Improved test coverage and explicitly documented parameter behaviors by running through simple static mock parameters rather than randomly generated variables or implicit variable coverage.
