## 2024-05-24 - Extracted helpers from load_klems_panel
**Learning:** Breaking down large complex functions (`load_klems_panel`) into smaller pieces like reading files and running index calculations improves code readability significantly.
**Action:** Always break down monolithic code blocks when refactoring into clear, single-responsibility helper functions.
